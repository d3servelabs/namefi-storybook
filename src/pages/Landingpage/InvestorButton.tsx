import { SolidButton } from '@components/Buttons/SolidButton'
import clsx from 'clsx'
import React from 'react'

export const InvestorButton = () => {
  return (
    <SolidButton backgroundColor={clsx('bg-red-400')}>
      
    </SolidButton>
  )
}
